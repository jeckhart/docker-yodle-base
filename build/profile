#!/bin/bash

# Meant to be imported by other shell scripts in the docker build

# Fail if any of these commands fails and show the commands that are executed
set -e
set -x

export DEBIAN_FRONTEND=noninteractive

apt_get='apt-get -y --no-install-recommends'
